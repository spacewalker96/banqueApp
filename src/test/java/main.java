import com.example.banqueApp.DAO.DAOCompte;
import com.example.banqueApp.DAOImpl.DAOImplCompte;
import com.example.banqueApp.model.Compte;

import java.sql.SQLException;

public class main {

    public static void main(String[] args) throws SQLException {
   /*     String sql = "SELECT * FROM banque.compte";


        Statement statement = null;
        try {
            statement = DbConfig.connect().createStatement();
            ResultSet  rs = statement.executeQuery(sql);
            if (rs.next()) {
                int rs1= rs.getInt("id");
                System.out.println(rs1);
            }

        } catch (SQLException throwables) {
            throwables.printStackTrace();
        }*/
        /*Compte compte = new Compte(2,120L,00.00,"nono");
        DAOCompte daoCompte = new DAOImplCompte(); {


        };
        daoCompte.allCompte();*/
    }

}
